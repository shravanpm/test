

export const TodoList = ({todo}) => {

    return (
        <div>
            <div>{todo.value}</div>
            <div><input type="checkbox" name="" onClick={()=>{}} /></div>
        </div>
    )
}